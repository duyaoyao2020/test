# Test project

A tiny Hello World program with a simple unit test.

## Run

```sh
python3 "Test project/hello_world.py"
```

## Test

```sh
python3 -m unittest discover -s "Test project"
```

## Third-party projects

The `third_party/` directory tracks useful external writing tools as Git submodules:

- `Humanizer-zh`
- `no-ai-slop`
